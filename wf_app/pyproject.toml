[project]
name = "wf-exporter-app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "databricks-sdk>=0.59.0",
    "flask>=3.1.1",
    "pyyaml>=6.0.2",
]
